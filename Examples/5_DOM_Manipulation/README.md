# JavaScript DOM Manipulation – Beispiele

Dieser Ordner enthält Beispiele zur DOM-Manipulation mit JavaScript.

## ▶️ Ausführen der Beispiele

1. Die gewünschte HTML-Datei auswählen (z. B. `index.html`)
2. Datei per Doppelklick im Browser öffnen
3. Aktionen (Buttons) anklicken
4. Änderungen im Browser beobachten

## 📌 Inhalte der Beispiele

- Ändern von Textinhalten über JavaScript
- Reaktion auf Benutzerinteraktionen (Click-Events)
- Dynamisches Erstellen und Einfügen von HTML-Elementen